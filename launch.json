{
    
    
   
    "inputs": [
        {
            "id": "DEBUG_URI",
            "type": "command",
            "command": "shellCommand.execute",
            "args": {
                "command": "cat ~PATH_OF_YOUR_APP/observatoryUri.txt",               
                "useFirstResult": "true",
            }
        }
    ],
    
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Flutter attach",
            "request": "attach",
            "type": "dart", 
             "preLaunchTask": "1 - FLUTTER IOS DEBUG (getting observatoryUri first)",
            "observatoryUri": "${input:DEBUG_URI}",                     
        },


    ]
}
